# Reference: 흐름 문서 출처

> `skills/universal-flow-document/SKILL.md`의 보조 문서다. 반입 출처·변경 범위 확인 시 읽는다.

사용자가 제공한 universal-flow-document 수집본을 기반으로 의미 모델과 JSON schema·예시 8개를 선별 반입했다. 원본의 SKILL·규칙은 하네스 규격에 맞춰 재작성하고, Python 실행기·검증기·HTML/CSS는 새로운 순수 Node 실행기와 DOM 기반 뷰어로 대체했다.

2026-09-22 사용자 지시에 따라 이 사용자 제공 자료에는 별도 라이선스 고지 문구를 추가하지 않는다. 출처와 변경 범위는 추적을 위해 유지한다.

원본의 Python 캐시, 벤더 배포 메타데이터, 리뷰 자체평가, 예시 HTML은 활성 스킬에 포함하지 않았다. 고정 해시·차용 파일·변경 범위는 `_SOURCE_MAPPING.md`에 기록한다.

후속 상세도 보강에서는 사용자 제공 chapchap-api-flowcharts.zip의 데이터·HTML과 수집본의 상세 규칙을 비교했다. 처리 주체·계약·실패·상태·탐색 표현을 범용 모델로 보강했으며 detailed-api 예제는 가상 계약으로 새로 작성했다. 첨부 제품의 128개 API 데이터를 활성 스킬이나 하네스 차트에 복제하지 않았다.
